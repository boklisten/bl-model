{
  "name": "@wizardcoder/bl-model",
  "version": "0.18.6",
  "description": "generic classes for use in all boklisten apps",
  "license": "MIT",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "prepare": "npm run build",
    "build": "tsc",
    "blink": "npm run build && cp ./package.json ./dist/ && npm link ./dist"
  },
  "dependencies": {
    "typescript": "^2.6.1"
  }
}
